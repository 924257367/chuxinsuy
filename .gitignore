_site
Gemfile.lock
.sass-cache
.jekyll-metadata
.DS_Store
.jekyll-cache
*.gem
git add .                      # 添加所有修改
git commit -m "更新描述"        # 提交到本地
git push origin main           # 推送到 GitHub（分支名根据实际情况替换）
